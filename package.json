{
  "name": "linkin-site-parking",
  "version": "1.0.0",
  "description": "사이트 주차장 - 카테고리별 링크 관리",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "init-db": "node -e \"require('./db').initPool(); require('./db').query('SELECT 1').then(() => console.log('DB OK')).catch(e => console.error(e))\""
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^3.6.5",
    "node-cron": "^4.2.1"
  }
}
